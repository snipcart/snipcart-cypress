{
  "scripts": {
    "test": "npm run browsersync & npm run cypress:open",
    "browsersync": "browser-sync start --server --files './**/*.html' --no-ui"
  },
  "devDependencies": {
    "browser-sync": "^2.27.9"
  }
}
